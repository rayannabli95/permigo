# Récap dev PermiGo — semaine du 8 au 15 mai 2026

## Résumé

Cette semaine on a bouclé la migration complète de v6 → v7 : les 23 pages prévues sont shippées (planning moniteur, livret REMC, réservation, dashboard admin, équipe, calendrier, profil, notifications, landing publique, etc.). Le code grossit de **+83 906 lignes pour −759 supprimées** sur 15 commits, dont l'`Initial commit` qui pose tout le scaffold. La prochaine priorité c'est de **durcir** : hash router (le reload casse la nav), dark mode, et premiers tests E2E.

## Commits par catégorie

**feat (1)**
- `07de800` — Logo + fix vidéo iOS + boutique + notifs

**fix / refactor (2)**
- `a28a346` — Update parcours.js
- `d900897` — Update login.js

**chore (12)**
- `4a2e772` — Initial commit (scaffold v7 complet)
- `9e881a8`, `7c78e61`, `835399f`, `530ca50`, `2ddf104`, `0cf58db`, `9ab7e48`, `4accc14`, `1a458a3`, `f1dfb4b`, `83b4dcc` — push (messages génériques, à améliorer la semaine prochaine)

## Statistiques de changement

| Métrique | Valeur |
|---|---|
| Commits | 15 |
| Lignes ajoutées | 83 906 |
| Lignes supprimées | 759 |
| Net | **+83 147** |
| Fichiers les + touchés | `signup.js`, `planning.js`, `aujourdhui.js`, `parcours.js`, `login.js`, `equipe.js`, `hero-key-scrub.js` (5 modifs chacun) |

## Déploiement

- **URL prod** : https://rayannabli95.github.io/permigo/ (redirige vers `permigo.vercel.app`)
- **Status HTTP** : 200 (récupéré via web_fetch — `curl -I` bloqué par allowlist sandbox)
- **Title** : "PermiGo — Une auto-école moderne, sans devenir une usine"
- **GitHub Actions** : `gh` non disponible dans l'environnement, étape skippée

## 3 suggestions pour la semaine prochaine

Toute la roadmap initiale est verte. Place au durcissement :

1. **Hash router** (`~60 min`) — supprime le bug "reload = redirige selon role". Pattern simple : `window.addEventListener('hashchange', route)` + `route()` au boot, chaque page lit `location.hash` au mount.
2. **Dark mode** (`~45 min`) — les variables CSS sont prêtes dans `src/styles/base.css`, manque le toggle dans `profil.js` et la persistance `localStorage`.
3. **Messages de commit propres** — 12 commits "push" cette semaine. Adopter une convention courte (`feat:`, `fix:`, `chore:`) facilitera les prochains recaps et un éventuel CHANGELOG auto.

## Notes du run automatique

- L'allowlist sandbox bloque `curl` direct ; le status prod a été récupéré via `web_fetch` (HTTP 200 OK, contenu rendu correctement).
- `gh` CLI absent → étape "dernier workflow Actions" non exécutée.
- CLAUDE.md mis à jour : section "Pages migrées" passée de 5 à 23 lignes, section "Pages à coder" remplacée par les pistes de durcissement.

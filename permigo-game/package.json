{
  "name": "permigo-game",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "PermiGo Game — l'app qui transforme le permis en habitude quotidienne",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vite --port 5173",
    "build": "vite build && node scripts/build-seo.mjs",
    "build:seo": "node scripts/build-seo.mjs",
    "preview": "vite preview",
    "lint": "node scripts/check-account-cache.mjs",
    "test": "playwright test",
    "test:ui": "playwright test --ui"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "@vercel/analytics": "^2.0.1",
    "posthog-js": "^1.376.0",
    "qrcode-generator": "^2.0.4",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.11.3",
    "@playwright/test": "^1.60.0",
    "vite": "^5.4.0"
  }
}

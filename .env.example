# ─── Environnement ───
NODE_ENV=development

# ─── Frontend (Vite expose via import.meta.env) ───
VITE_SUPABASE_URL=https://ivtuheoyfgljujliscwf.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...remplace_avec_ta_cle_anon
VITE_API_URL=http://localhost:3001

# ─── Cloudflare Turnstile (optionnel — captcha invisible) ───
# 1. https://dash.cloudflare.com/?to=/:account/turnstile → Add Site
# 2. Widget mode = Managed, ajoute rayannabli95.github.io + localhost
# 3. Colle la SITE KEY ici (sitekey publique, safe à commit)
# 4. Colle la SECRET KEY dans Supabase → Auth → Settings → Bot Protection → Turnstile
VITE_TURNSTILE_SITEKEY=

# ─── Backend (Node) ───
# Dev : SQLite local
DATABASE_URL=file:./dev.db
# Prod : Postgres Supabase
# DATABASE_URL=postgres://postgres.ivtu...:PASSWORD@aws-0-eu-west-3.pooler.supabase.com:5432/postgres

# Service role pour ops admin côté serveur uniquement
SUPABASE_SERVICE_ROLE_KEY=eyJ...JAMAIS_committer_cette_cle

# Port du backend Hono
PORT=3001

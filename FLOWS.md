# FLOWS.md — Parcours cross-rôles PermiGo

> **Source de vérité des scénarios end-to-end** qui impliquent plusieurs rôles. Si tu codes côté élève, tu lis ici ce que le moniteur t'envoie et inversement.
> Schéma DB technique = `src/db/schema.js` (ne dupliquer ici aucun champ).

## Vocabulaire

- **REMC** : Référentiel d'Éducation à la Mobilité Citoyenne — 31 sous-compétences × 4 catégories
- **Leçon** : `events.t ∈ {conf, lecon, pend}` (conf=confirmée, pend=en attente, lecon=passée)
- **Dispo** : `events.t = 'dispo'` (créneau libre proposé par moniteur)
- **Forfait** : `profiles.forfait_h` heures payées par l'élève

---

## Flux 1 — Cycle d'une leçon

```
[ÉLÈVE]                    [MONITEUR]                  [DB]
   │                            │                       │
   │ ouvre /reservation         │                       │
   │ ──► lit events t='dispo'   │                       │
   │                            │                       │
   │ click créneau              │                       │
   │ ──► insert events t='pend' │                       │
   │     eleve_id=me            │ ──► notif "Demande   │
   │                            │       de leçon"       │
   │                            │ confirme              │
   │                            │ ──► update t='conf'   │
   │ ──► notif "Confirmée"      │                       │
   │                            │                       │
   │     ⏰ jour J               │     ⏰ jour J          │
   │                            │                       │
   │                            │ post-leçon :          │
   │                            │ ──► update t='lecon'  │
   │                            │     remc_entries (lv) │
   │                            │     lesson_reviews    │
   │ ──► notif "Leçon notée"    │                       │
   │ ouvre /accueil             │                       │
   │ ──► voit dernière review   │                       │
   │     prompt rating moniteur │                       │
   │ ──► insert notations       │                       │
   │                            │ voit étoiles ⭐        │
```

**Contrat technique :**
- Élève → écrit `events` (`t='pend'`), `notations`, `lesson_self_evals`
- Moniteur → écrit `events` (`t='conf'|'lecon'`), `remc_entries`, `lesson_reviews`, `notes_priv`
- Notifications déclenchées : voir Flux 4

## Flux 2 — Progression REMC

```
[MONITEUR]                                [ÉLÈVE]
   │                                          │
   │ /livret-remc?id=<eleve>                  │
   │ click case "C1a" → niveau 'v'            │
   │ ──► upsert remc_entries                  │
   │     {eleve_id, comp_id:'C1a', lv:'v'}    │
   │                                          │
   │                                          │ /parcours
   │                                          │ select remc_entries where eleve_id=me
   │                                          │ ──► node "C1a" se déverrouille
   │                                          │     XP +100, confetti, world-unlock
```

**Contrat :**
- `remc_entries.lv` valeurs autorisées : `'v'` (acquis) | `'p'` (en cours) | `'r'` (à retravailler) | `null` (pas évalué)
- `comp_id` doit exister dans `src/data/remc.js` (REMC officiel)
- Total = 31 compétences (constante `REMC_TOTAL`)

## Flux 3 — Forfait / heures consommées

```
[ADMIN]                       [MONITEUR]                  [ÉLÈVE]
   │                              │                          │
   │ /eleves                      │                          │
   │ update profiles.forfait_h    │                          │
   │                              │ /mes-eleves              │
   │                              │ voit heures restantes    │
   │                              │                          │ /accueil
   │                              │                          │ KPI "restantes"
   │                              │                          │ = forfait_h
   │                              │                          │   - SUM(events.dur)
   │                              │                          │     WHERE t IN ('conf','lecon')
```

**Contrat :** la formule "heures restantes" doit rester IDENTIQUE dans les 3 vues. Si elle change, mettre à jour `src/services/planning.js` (helper partagé à créer si manquant).

## Flux 4 — Notifications cross-rôles

Toute écriture qui doit notifier quelqu'un d'un autre rôle passe par `notifications`.

| Trigger (écrivain) | Destinataire | type | title |
|---|---|---|---|
| Élève réserve (`events.t='pend'`) | moniteur de l'event | `lecon_demande` | "Nouvelle demande de leçon" |
| Moniteur confirme (`events.t='conf'`) | élève | `lecon_confirmee` | "Leçon confirmée" |
| Moniteur passe à `t='lecon'` + crée review | élève | `lecon_terminee` | "Ta leçon est notée" |
| Moniteur valide compétence (`remc_entries.lv='v'`) | élève | `comp_acquise` | "Compétence validée 🎉" |
| Admin modifie `profiles.forfait_h` | élève + moniteur attitré | `forfait_maj` | "Ton forfait a été mis à jour" |
| Élève note moniteur (`notations`) | moniteur | `nouvelle_note` | "Tu as une nouvelle note ⭐" |

**Règle d'or :** chaque bot écrit la `notification` au moment de l'action métier. Pas de cron, pas de trigger DB pour l'instant (KISS).

## Flux 5 — Privé vs partagé

- `notes_priv.contenu` : **JAMAIS** affiché côté élève. Si un bot élève fait un `SELECT` sur cette table, c'est un bug — l'élève ne doit même pas savoir qu'elle existe.
- `audit_log` : visible admin uniquement.
- `absences` : invisible élève.

---

## Changements en cours (bots actifs annoncent ici)

> Format : `[YYYY-MM-DD bot-X] changement | impact | status`

- `[2026-06-14 bot-moniteur]` **Ajout additif** `permigo-game/src/utils/fmt-name.js` (`fmtName` — Title Case d'affichage, ne touche pas la DB) | importé seulement par des pages `src/pages/enseignant/**` (log-session, aujourdhui, mes-eleves, classement-eleves, bilan) | done. Réutilisable par les bots élève/admin pour normaliser l'affichage des noms.
- `[2026-06-14 bot-moniteur]` **Lecture seule** `quiz_attempts` (user_id, score, completed_at) ajoutée dans `classement-eleves.js` pour la nouvelle « Ligue théorie » (mode `#/classement-eleves/theorie`) — même requête RLS que la KPI « Taux quiz » d'Analyses | aucun écrit | done.
- `[2026-06-16 auth]` **Récupération d'accès élève déclenchée par le moniteur** : edge function `eleve-recovery` (verify_jwt, **DÉPLOYÉE**) — authz enseignant/gérant + élève de la même `auto_ecole` → envoie un email de connexion (OTP/lien) à l'élève (le moniteur ne voit jamais le lien). Bouton « Réinitialiser l'accès » dans le quick-menu de `mes-eleves.js` + `src/services/eleve-recovery.js`. ⚠️ Envoi fiable nécessite SMTP custom (gratuit, Resend) — sinon limites email par défaut. **Pas de Pro requis.** | Impact : moniteur déclenche, élève reçoit.
- `[2026-06-16 bot-eleve]` **Hall of Fame élève** (PR ouverte, NON appliquée/mergée) : modifie le RPC partagé `get_eleve_leaderboard` (exclut les lauréats `examens.statut='recu'` du classement) + nouveau RPC `get_hall_of_fame(p_scope)` (renvoie le **prénom réel** des lauréats, école-scoped). Décision produit validée Rayan : prénom des reçus exposé aux camarades d'école. À appliquer + merger ensemble (matin). Impact : classement élève (et indirectement la lecture moniteur via examens — lecture seule, pas de changement moniteur).
- `[2026-06-15 billing]` **Stripe Checkout (abonnement 9,99 €/mois moniteur indé)** : nouvelle table `subscriptions` + RLS (miroir Stripe, écrit par webhook en service role, lecture user only) ; edge functions `stripe-checkout` + `stripe-webhook` ; `src/services/billing.js` ; section Abonnement dans `settings.js` (rôle enseignant). **NON déployé / migration NON appliquée** (besoin compte Stripe + secrets — voir `permigo-game/docs/STRIPE-SETUP.md`). | Impact : moniteur (self-serve) + DB | scaffold done, PR ouverte.
- `[2026-06-15 night-run]` **Conversion A2HS (install écran d'accueil)** : `pwa.js` (additif : `isInAppBrowser`/`isIosNonSafari`/`installBlockedReason`), `install-nudge.js` (variante « ouvre dans navigateur » pour webviews/iOS non-Safari + exports `openInstallSheet`, `promptInstallAtValueMoment`), `settings.js` (entrée « Ajouter à l'écran d'accueil »), `log-session.js` (prompt install au succès de validation). Tout additif, aucune écriture DB. | Impact : tous rôles (boot + réglages) + moniteur (moment de valeur) | done (PR à ouvrir).
- `[2026-06-18 bot-eleve]` **Récap fin de session Révision (Clash Royale)** : composant **additif** `src/components/eleve/revision-recap.js` (`showRevisionRecap`, z-index 10050, visuel de dépassement d'un autre élève) + service `src/services/revision-session.js` (session sessionStorage `permigo:revision_session_v1`, TTL 3h, capture rang Révision au 1er quiz, compteurs). Déclenché dans `quiz.js` quand l'élève quitte l'enchaînement révision (`#btn-parcours` en mode `isRevision`) → `#/parcours` (ou `#/classement/revision` via lien secondaire). **Lecture seule** : appelle le RPC partagé `get_theory_leaderboard(p_scope='ecole')` (déjà utilisé par classement.js), aucune écriture DB. Impact : élève (quiz révision) uniquement.
- `[2026-06-18 bot-eleve+moniteur]` **Écran plein écran de célébration (Nike/COD/Strava)** : moteur générique **additif** `src/components/common/unlock-screen.js` (`showUnlockScreen`, z-index 10050). Wrappers de rôle : `src/components/eleve/competence-unlock.js` (`showCompetenceUnlock` — refactoré pour déléguer au moteur, **API/comportement inchangés**) ; `src/components/enseignant/tier-unlock.js` (`showTierUnlock` — palier moniteur). Déclencheurs : élève = quiz `validated` (quiz.js) + parcours-driven (`src/services/competence-celebration.js`) ; moniteur = franchissement seuil `MONITEUR_TIERS` au succès de `validate_session` (`log-session.js` → `src/services/moniteur-tier-celebration.js`, ledger localStorage, seed silencieux). Lecture seule (count `validations`), aucune écriture DB. ⚠️ **Décision produit Rayan (2026-06-18)** : l'écran moniteur réutilise le style premium élève — dérogation **assumée** à l'antipattern moniteur « pas de confetti/Duolingo ». NE PAS « corriger » en sobre sans redemander. | Impact : élève (parcours/quiz) + moniteur (log-session) | done (non commité).
- `[2026-06-14 bot-moniteur]` **Coordination overlays 1er lancement** : nouvel util partagé `permigo-game/src/utils/intro-overlays.js` + `main.js` (appel `armPopupPhase()` avant `route()`). Le tuto guidé (élève `accueil.js` + enseignant `aujourdhui.js`) attend désormais que le popup d'engagement soit fermé (`onPopupsSettled`). `install-nudge.js` / `push-prime.js` signalent `notifyPopupOpen/Settled` (l'ancien deferral popup→tuto, racy, est retiré). | Impact : élève + enseignant + boot partagé | done.

- `[2026-06-22 permigo-eleve-dev]` **Variete mascotte quiz** : modif **additive** `src/components/eleve/quiz-ui.js` (fonction `setMascot` enrichie d'un micro-pop CSS — ajout classe `.qz-mascot--pop`, aucun changement de signature, rétrocompat) + `src/pages/eleve/exam-blanc.js` (mascotte reactive dans `renderQuestionBody` / `reveal()` : switch pose think/celebrate/coach selon état réponse, playWhoosh sur nextQ). Aucune écriture DB, aucun nouveau son. | status: in-progress
- `[2026-07-10 permigo-eleve-dev]` **Hub « Récompenses » (chantier 2 nav simplifiée, maquette validée)** : nouvelle page `permigo-game/src/pages/eleve/recompenses.js` (route `#/recompenses`) qui réunit la Roue (hero, réutilise `roue_daily_spins`/`get_moniteur_rewards`) + 4 onglets internes résumé→« Tout voir » (Boutique, Ma collection, Trophées) + Ligue (monte tel quel le composant partagé `src/components/eleve/league-hero.js`, déjà utilisé par accueil.js — zéro nouvelle logique). **Modif partagée additive** `permigo-game/src/router.js` (ajout route `recompenses` dans le bloc `// eleve` uniquement) + `permigo-game/src/components/common/nav-bottom.js` (l'onglet élève `id:"boutique"` devient `id:"recompenses"`, cible `#/recompenses`, `match` élargi à `["boutique","galerie","trophees","mes-coffres","classement","roue"]` ; les 2 sélecteurs de la pastille trophées `.bn-tab[data-id="boutique"]` → `[data-id="recompenses"]`). Aucune route existante retirée (`#/boutique`, `#/galerie`, `#/trophees`, `#/classement`, `#/mes-coffres`, `#/roue` inchangées, toujours accessibles en direct/deep-link). Lecture seule DB (aucune nouvelle table/colonne). | Impact : nav élève uniquement (le moniteur/gérant ont leurs propres blocs `TABS`, non touchés) | status: DONE
- `[2026-07-11 permigo-eleve-dev]` **Hub « Mon permis » (chantier 5 — DERNIER de la nav simplifiée, maquette `nav-mon-permis-A.html` validée, variante A « La Route »)** : nouvelle page `permigo-game/src/pages/eleve/mon-permis.js` (route `#/mon-permis`) — timeline 3 étapes (grammaire Carnet, numéros violets + filet) : ① Mes compétences (chip « Suivi par {prénom} · validé en leçon » via `profiles.enseignant_id`, hero « X/31 » + 4 chapitres C1-C4, cumul « X h de conduite · Y leçons » = somme `sessions_moniteur.duration_minutes` où `confirmation_status IN ('confirmed','auto')` — un CUMUL, jamais un crédit) ; ② Mes leçons (dernier `comptes_rendus` + lien « Toutes mes leçons » → nouvelle page `permigo-game/src/pages/eleve/mes-lecons.js`, route `#/mes-lecons`, historique complet, chaque ligne → `#/compte-rendu/{id}` existant) ; ③ L'examen (countdown + « Modifier la date » via le mécanisme localStorage EXISTANT d'`examen.js`, verdict 3 niveaux + checklist 4 critères — zéro nouveau seuil) ; entrée « Ton centre d'examen » → `#/centre-examen` dans son état actuel (`CENTRES_PREMIUM_LOCKED=false` → pas de pastille PermiGo+, la maquette montre un état futur non codé). **DÉCISION PRODUIT FERME reflétée dans le code** : le jeu (mondes/boss/coffres, `#/parcours`) et le sérieux sont séparés — `#/parcours` reste intact, accessible par Réviser → « Jouer », n'apparaît plus dans « Mon permis ». Réutilisation par **imports dynamiques** (jamais statiques — parcours.js/accueil.js sont de gros chunks avec chest/sheet-swipe/league-hero/heatmap ; un import statique les aurait fusionnés dans le chunk du hub) : `computeWorldStates` **exportée** de `parcours.js` (mêmes seuils C1-C4, zéro réécrit) ; `loadData/buildCriteria/buildVerdict/parseSavedDate/saveExamDate/countdown/fmtDate/BASE_TOTAL` **exportés** de `examen.js` (`buildVerdict` = extraction de la logique verdict de `renderChecklist`, comportement inchangé — la readiness reste gelée, un seul calcul dans toute l'app) ; `fetchLastCompteRendu` **exportée** de `accueil.js` (même table/tri que la bannière accueil, sans le filtre `read_at IS NULL`). **Modif partagée additive** `permigo-game/src/router.js` (ajout `mon-permis`/`mes-lecons` dans le bloc `// eleve` uniquement + `ROUTE_TITLES`) + `permigo-game/src/components/common/nav-bottom.js` (l'onglet élève `id:"parcours"` devient `id:"mon-permis"`, cible `#/mon-permis`, `match` élargi à `["examen","centre-examen","compte-rendu","sessions","mes-lecons"]` ; l'onglet `id:"reviser"` gagne `"parcours"` dans son `match` — cohérent avec la séparation jeu/sérieux, `#/parcours` allume maintenant Réviser) + `permigo-game/src/pages/common/notifications.js` (le type `comp_acquise`, élève-only, route maintenant vers `#/mon-permis` au lieu de `#/parcours` — vraie progression validée, pas le jeu ; aucun autre type/rôle touché). **Décisions cross-liens documentées** (mission) : `#/examen` reste une page autonome intacte (deep-links/notifs existants inchangés) — le CTA « Prépare ton examen » de `parcours.js` (seul lien historique vers `#/examen`) pointe maintenant vers `#/mon-permis?scroll=exam` (auto-scroll à l'étape ③) ; le CTA « Voir mon itinéraire » de `compte-rendu.js` (+ ses 2 fallbacks d'erreur) pointe maintenant vers `#/mon-permis` au lieu de `#/parcours` (un compte-rendu de leçon est un événement sérieux) ; le tuto onboarding élève (`accueil.js`, sélecteur `.bn-tab[data-id="parcours"]`) suit le renommage → `[data-id="mon-permis"]`. Aucune route existante retirée (`#/parcours`, `#/examen`, `#/centre-examen`, `#/compte-rendu/{id}`, `#/sessions` tous inchangés/valides). Lecture seule DB (`validations`, `profiles`, `sessions_moniteur`, `comptes_rendus`) — aucune nouvelle table/colonne, aucun écrit. | Impact : nav élève uniquement (moniteur/gérant ont leurs propres blocs `TABS`/`ROUTES`, non touchés) | status: DONE
- `[2026-07-16 permigo-eleve-dev]` **Validation autonome des compétences (élève SANS moniteur, pré-vente Pass Permis)** : nouvelle table `self_validations` (migration `permigo-game/supabase/migrations/20260716150000_self_validations.sql`, **NON appliquée en prod** — à la charge de l'utilisateur) + RPC `self_validate_competence(p_competence_id, p_score)` SECURITY DEFINER — seule porte d'écriture (aucune policy INSERT/UPDATE client), refuse si l'élève a un moniteur (`profiles.enseignant_id`) ou si la compétence est déjà `validations.statut='acquis'` (jamais d'écrasement de la source de vérité moniteur), exige score ≥80%. Table **complètement séparée** de `validations` — zéro ligne attribuée à un enseignant, zéro impact stats moniteur. Nouvelle page `permigo-game/src/pages/eleve/valider-seul.js` (route `#/valider-seul/{compId}`) : rappel de fiche + lien vers `#/revision-conduite/{compId}`, puis quiz de validation réutilisant le moteur DB existant (`quiz-engine.js`, `lancerQuiz`, questions `type='post_validation'`, 5 questions) — réussite ≥80% → RPC ci-dessus, échec → écran d'encouragement. **Modif partagée additive** `permigo-game/src/router.js` (ajout `valider-seul` dans le bloc `// eleve` uniquement + `ROUTE_TITLES`). `permigo-game/src/pages/eleve/parcours.js` (openFiche) : fusionne `self_validations` dans `validatedMap` en LECTURE SEULE (débloque mondes/étapes comme une validation moniteur, sans jamais écraser une validation moniteur existante) + CTA « Ton moniteur te l'a validée en leçon ? Valide-la ici. » affiché uniquement si `!hasMoniteur`, badge distinct « Auto-validée » sur les compétences validées via ce chemin. `permigo-game/src/pages/eleve/mon-permis.js` : même fusion en lecture seule pour que la progression du hub reste juste pour un compte solo. Aucune table/colonne moniteur touchée, aucune page `enseignant/**`/`admin/**` touchée. | Impact : élève solo uniquement (moniteur/gérant non affectés) | status: DONE (front) / migration EN ATTENTE d'application prod
- `[2026-07-10 permigo-moniteur-dev]` **Hub « Mes élèves » (chantier 3 nav simplifiée, maquette `nav-mes-eleves-A.html` validée)** : `permigo-game/src/pages/enseignant/mes-eleves.js` devient un hub à 3 onglets internes — **Liste** (pipeline existant, inchangé, enrichi d'une cloche sur les lignes « à relancer » → saute vers l'onglet Relances) · **Relances** (monte `relances.js` refactoré en fonctions exportées `computeCooling`/`renderRelanceCard`/`wireRelanceCards`/`STYLE` — zéro requête supplémentaire, calculé sur `_eleves` déjà chargé par le hub) · **Classement** (monte `fetchRanking()` exporté de `classement-eleves.js` — même fetch/tri, mais re-skinné en léger « Pupitre » au lieu du skin Arène nuit, pour coller à la maquette validée et rester cohérent avec Liste/Relances ; chargé à la demande au 1er accès à l'onglet, mis en cache par mode pratique/théorie). Le hero « à traiter en priorité » est retiré, remplacé par 3 compteurs vivants (élèves en formation / à relancer / prêts exam), chacun une porte vers le bon onglet. Bande « Reçus » recolorée violet→or (DA moniteur unifiée). **Modif partagée additive** `permigo-game/src/router.js` (les clés `relances` et `classement-eleves` du bloc `// enseignant` pointent maintenant vers `mes-eleves.js` — même pattern que l'alias `validation` → `log-session.js` déjà en place ; `mes-eleves.js` lit `location.hash` lui-même pour choisir l'onglet, donc AUCUNE ancienne URL ne casse : `#/relances`, `#/classement-eleves`, `#/classement-eleves/theorie` atterrissent sur le hub avec le bon onglet/sous-mode) + `permigo-game/src/components/common/nav-bottom.js` (l'onglet enseignant `id:"eleves"` gagne `match:["relances","classement-eleves","bilan"]` pour rester allumé sur ces routes — avant cette modif, aucun onglet ne s'allumait sur ces 3 routes, bug pré-existant maintenant corrigé). Aucune route retirée, aucune écriture DB nouvelle, `classement-eleves.js`/`relances.js` gardent leur `mount()` standalone intact (defensive, non référencé par le router mais toujours exporté/fonctionnel). | Impact : nav moniteur uniquement (élève/gérant ont leurs propres blocs `TABS`/`ROUTES`, non touchés) | status: DONE
- `[2026-08-06 notifs-copy]` **Épuration des textes de notifications (zéro point final, zéro tiret, une idée par notif)** : `permigo-game/src/pages/common/notifications.js` (**FICHIER PARTAGÉ élève + moniteur**) — dictionnaires i18n EN/AR de la coque et des gabarits émotionnels raccourcis, plus 2 replis FR (`empty_body`, `load_err_sub`). ⚠️ Une seule modif de LOGIQUE : le découpage du corps `post_validation_quiz` acceptait uniquement le tiret cadratin (`split("—")`) — il accepte maintenant `—`, `–`, `·` et ` - `, et retombe sur le corps entier si aucun séparateur n'est trouvé (avant, il dupliquait le corps). Aucun type moniteur touché (`student_at_risk`, `moniteur_recap`, `session_*` restent en repli FR serveur), aucune route, aucune écriture DB. Également `permigo-game/src/components/common/push-prime.js` (partagé élève/moniteur) : 3 sous-titres FR/EN/AR perdent leur point final, aucun changement de logique. Côté serveur **modifié dans le dépôt mais NON DÉPLOYÉ** : `supabase/functions/{dispatch-push,trigger-consolidation,weekly-recap-eleve,smart-reengagement,send-emotional-nudge,monthly-recap-moniteur}/index.ts` + `permigo-game/public/sw.js`. Aucune migration SQL touchée. | Impact : page notifications (2 rôles) + copies push | status: DONE (front) / edge functions EN ATTENTE de déploiement
- `[2026-08-06 permigo-eleve-dev]` **Passe de nettoyage audit visuel élève (5 correctifs)** : modif **additive et role-gated** `permigo-game/src/components/common/header-top.js` — (1) le logo (`#ht-logo`) ne reste visible que sur Accueil (`#/`) et Boutique (`#/boutique`) pour le rôle `eleve` uniquement (`syncLogoVisibility()`, appelée au mount + sur `hashchange` ; `visibility:hidden` pas `display:none`, pour ne pas casser le `justify-content:space-between` du header) — moniteur/gérant/owner **inchangés**, le logo reste affiché sur toutes leurs pages comme avant ; (2) la pastille volants (`#ht-volant-btn`) ouvre désormais un petit aperçu (`openVolantPop`/`closeVolantPop`, solde + « Voir la boutique ») avant la redirection vers `#/boutique`, fermeture au clic extérieur ou au `hashchange`. Aucun changement de comportement pour moniteur/gérant (le bouton volants n'existe que pour `role==='eleve'`, code déjà existant). Le reste des correctifs vit exclusivement dans `src/pages/eleve/**` (scope exclusif) : `en-situation.js` (mélange Fisher-Yates des réponses au rendu, `withShuffledReponses` — la donnée `data/situations-conduite.js` n'est pas touchée, la correction reste par ID donc rien à remapper), `recompenses.js` (kicker du hero « La Roue » reformulé pour ne plus répéter le mot avec le CTA « Lancer la roue »), `revision-conduite.js` (bandeau `.fd-intro` recentré + texte plus affirmé, prénom élève en `<strong>` via un jeton pré-esc() jamais un tag injecté dans du HTML déjà échappé). Aucune écriture DB, aucune nouvelle route. | Impact : header partagé (élève seulement, autres rôles inchangés) + 3 pages élève exclusives | status: DONE
- `[2026-08-06 permigo-eleve-dev]` **Source unique de la série (streak) + cadre avatar profil** : la série affichait 3 chiffres différents (accueil « 6 j », profil « 0 j », calendrier « En cours : 6 ») car 3 calculs distincts existaient sur `streaks` — accueil.js gardait `current_streak` BRUT (jamais remis à 0 même série cassée) + bump optimiste ; profil.js remettait à 0 si `last_activity_date` périmée mais SANS le bump ; reviser.js lisait `utils/game-state.js#getStreak()`, un cache localStorage figé au boot de l'app (jamais rafraîchi). **Racine confirmée** : `current_streak` n'est JAMAIS remis à 0 par le serveur (cf. `get_today_quests()`, migration 20260709120000) → un vieux nombre reste affiché tel quel tant qu'aucun calcul de fraîcheur n'est appliqué. **Nouveau fichier additif** `permigo-game/src/services/streak.js` : règle unique (série cassée = `current` affiché à 0, `longest` jamais remis à 0, bump optimiste centralisé) exposée en 2 niveaux — helpers purs synchrones `getStreakStatus`/`computeEffectiveStreak` (pour accueil.js, qui a déjà `streaks`+`quiz_attempts` en main, zéro requête réseau en plus) et `getStreak()` async avec cache mémoire par user id (pour profil.js/reviser.js/recompenses.js, qui n'avaient pas cette donnée sous la main). **Consommateurs migrés** : `src/pages/eleve/accueil.js` (fonction locale `streakStatus()` supprimée, remplacée par l'import partagé), `src/pages/eleve/reviser.js` et `src/pages/eleve/recompenses.js` (import basculé de `utils/game-state.js#getStreak()` vers `services/streak.js#getStreak()` — `utils/game-state.js#getStreak()` n'est PAS supprimé, laissé en place au cas où, mais n'a plus aucun appelant), `src/pages/common/profil.js` **FICHIER PARTAGÉ élève/moniteur** — modif strictement confinée à `mountEleveArene()` (le chemin élève, `me.role==='eleve'`), le chemin `mountEnseignantArene()` n'est pas touché. Détail navré : `_neverDidAnything`/`isFirstRun` dans accueil.js utilisaient `!streak.current_streak` pour détecter un tout nouvel élève — basculés sur `!streak.longest_streak` (jamais remis à 0) pour ne pas traiter un élève revenant après une pause comme un nouveau. **Bonus même passe** (bug visuel, capture Rayan) : `src/components/eleve/activity-heatmap.js` (composant élève-only, un seul appelant `accueil.js`) — (a) nouveau paramètre additif `showStats` (défaut `true`, rétrocompat) pour ne plus afficher deux fois « X jours actifs » quand la page appelante a déjà son propre en-tête (accueil.js passe maintenant `showStats:false`) ; (b) `.hmap-months` (étiquettes de mois) n'avait pas le même `grid-template-columns` que `.hmap-grid` (la CSS retombait sur un défaut de 14 colonnes jamais posé via `--cols`, alors que le vrai nombre de colonnes = `weeks`, 5 dans la bottom sheet) → étiquettes désalignées avec les cases en dessous, corrigé en calculant le même `grid-template-columns` inline pour les deux grilles. **Cadre avatar profil** (`src/pages/common/profil.js`, `.arn-crest*`, chemin élève uniquement) : l'ancien anneau extérieur utilisait un pseudo-élément en `mask-composite` (CSS moderne peu fiable niveau anti-aliasing) qui ne suivait pas exactement la même courbe que le disque en dessous → liseré visible. Remplacé par 2 anneaux concentriques peints chacun en un seul passage (padding + fond dégradé, même technique que le disque lui-même, déjà fiable) avec des rayons calculés pour rester parfaitement concentriques (26 → 24 → 21). Aucune écriture DB dans toute cette passe (uniquement des lectures `streaks`/`quiz_attempts` déjà existantes, réorganisées). | Impact : 4 pages élève (accueil, réviser, récompenses, profil-élève) + 1 composant élève-only + 0 changement moniteur/gérant | status: DONE
- `[2026-08-06 permigo-eleve-dev]` **Repli sur avatar manquant/introuvable (jamais l'icône « image cassée »)** : `permigo-game/src/components/common/avatar.js` **PARTAGÉ tous rôles** — `renderUserAvatar()` pose désormais l'`<img>` de l'avatar_url ET son repli initiales (déjà « l'avatar par défaut » quand aucun avatar_url n'existe) dans le MÊME balisage, repli masqué par défaut, révélé par `onerror` si le fichier est introuvable (idiome déjà utilisé ailleurs dans le projet : `display:none` + `nextElementSibling.style.display`). Signature et rendu inchangés dans le cas nominal (avatar valide) → **rétrocompatible**, tous les appelants existants (header-top.js, classement.js, arene-rank.js, league-shared.js, pages enseignant) sont corrigés gratuitement sans y toucher. **Modif isolée au chemin élève** `permigo-game/src/pages/common/profil.js` **FICHIER PARTAGÉ élève/moniteur** — le bouton avatar 44px `.arn2-av` de `mountEleveArene()` a son propre balisage bespoke (pas le helper générique, pour garder son gabarit carré/14px de rayon) : même idiome onerror appliqué localement, `mountEnseignantArene()` non touché. Cause du bug observé : `profiles.avatar_url` de `eleve@test.fr` pointe vers `/skins/avatars/garcon-hoodie-gris.webp`, fichier absent de `public/skins/avatars/` et jamais référencé dans `src/` — valeur orpheline en base, **non modifiée** (DB read-only dans cette passe). Aucune écriture DB, aucune nouvelle route. | Impact : helper avatar partagé (tous rôles) + profil élève (chemin élève uniquement) | status: DONE
- `[2026-08-07 permigo-eleve-dev]` **Refonte de l'inscription élève en 4 écrans mascotte (direction A validée par Rayan, `mockups/inscription-3-ecrans/A-mascotte.html`)** : `permigo-game/src/pages/public/rejoindre.js` **PARTAGÉ public** (porte `#/rejoindre` self-serve, `?solo=1` compte gratuit, `?code=` pré-rempli) réécrite en profondeur : composition unique répétée sur 4 écrans (mascotte + bulle en haut, question en gros, champ « ligne » épuré) → ① prénom seul (`set_my_identity`, colonne `profiles.prenom` déjà existante) ② email + mot de passe (le mot de passe reste, décision confirmée : l'OTP par code email est désactivé côté serveur pour ce projet et un lien magique ouvrirait le navigateur au lieu de l'app installée) + en mode rejoindre-par-code (pas `solo=1`) un 3e champ « Code moniteur » en retrait sous le mot de passe, même RPC `get_join_code_info`/`join_moniteur_by_code` qu'avant, comportement et messages d'erreur inchangés (`invalid_code`/`already_has_school`) ③ boîte de vitesses (PASSABLE, `utils/transmission.js#enregistrerBoite`, colonne `profiles.transmission` déjà existante) ④ arrivée avec le prénom saisi. Compte créé à la fin de l'écran ② (`sb.auth.signUp`, session active immédiatement en prod) ; `restoreSession()` (déjà exportée de `auth.js`) rappelée juste après pour que `getCurUser()` soit à jour DANS la page (sans reload) et que l'écran boîte puisse écrire. Le sélecteur de langue et le bandeau « déjà connecté » de l'ancien formulaire sont retirés de l'écran principal : la langue est déjà auto-détectée au boot (`utils/lang.js#browserLang()`) et réglable dans `#/settings` (déjà présent) — la maquette validée n'a que 4 écrans, pas 5 ; l'état « déjà connecté » (session existante sur le même téléphone) devient un écran dédié simple (mascotte + message + bouton déconnexion), plus la maquette Typeform. **Corollaire direct demandé par la mission** : `permigo-game/src/pages/onboarding/index.js` (scope élève, PR #725 du 06/08 défaite) — la section « Ta boîte de vitesses » entièrement retirée (import `transmission.js`, `showBoite`/numérotation, bloc HTML, wiring, tracking, CSS dark+light) puisqu'elle est désormais demandée une fois, plus tôt, dans `rejoindre.js` ; le reste de l'onboarding (avatar, couleur, rappels, A2HS) est inchangé. **Décision documentée, `src/pages/public/signup.js` (lien d'invitation `#/signup?token=…`) volontairement NON touché** : ce formulaire est partagé élève ET enseignant (invitations depuis `gerant/equipe.js`) et porte des champs légaux absents de la maquette (date de naissance, consentement parental) — le réécrire pour coller à la maquette élève aurait cassé le circuit enseignant et fait sauter une obligation légale. Aucune migration DB (toutes les RPC/colonnes utilisées existaient déjà). Testé en réel (2 comptes `@permigo-qa.invalid` jetables, solo + code `RAYAN75`, géométrie vérifiée à 375×667 et 390×844, focus au montage, retour sans perte de saisie, comptes supprimés via `delete_my_account` en fin de test). | Impact : porte publique élève (partagée) + onboarding élève (scope élève) | status: DONE
- `[2026-08-06 permigo-eleve-dev]` **Retrait de `#/cartes` de la navigation générale élève (doublon avec le profil)** : suite à la refonte profil du 06/08 (entrée juste au-dessus) qui a fait du profil élève la maison du paquet de cartes, `#/cartes` (et son alias `#/trophees`) restaient accessibles comme un 2e endroit de consultation via plusieurs portes. **Décision** : la route `#/cartes/{compId}` **survit uniquement comme écran de révélation** posé par `valider-seul.js` après une certification (confetti + swipe deck complet réutilisé tel quel) — elle n'est plus un onglet de nav générale. Sans id, ou avec un id qui ne correspond à aucune compétence débloquée, `permigo-game/src/pages/eleve/collection.js#mount` redirige vers `#/profil` (`navigate()`, avant tout fetch pour le cas sans id). `#/trophees` (ancien alias de la même page depuis le 30/07, jamais porteur d'id dans aucun flux du code) est retirée via le mécanisme existant `ROUTES_RETIREES` de `permigo-game/src/router.js` **PARTAGÉ** → redirige vers `#/profil`. Portes traitées : `permigo-game/src/components/eleve/recompenses-tabs.js` (scope élève, la salle « Cartes » du bandeau des 4 pages Boutique/Ma collection/Cartes/Classement retirée, 3 salles restantes) ; `permigo-game/src/pages/eleve/recompenses.js` (scope élève, les 4 liens « Tout voir mes trophées »/« Trophée à réclamer » pointent maintenant vers `#/profil`) ; `permigo-game/src/pages/common/notifications.js` **PARTAGÉ élève/moniteur** (type `achievement_unlocked`, élève-only, `#/trophees` → `#/profil`) ; `permigo-game/src/components/common/nav-bottom.js` **PARTAGÉ tous rôles** (onglet élève « Récompenses » perd `cartes`/`trophees` de son `match`, l'onglet « Profil » gagne `match:["cartes"]` pour rester allumé sur l'écran de révélation). Fichier orphelin repéré mais **non touché** (hors scope, pré-existant depuis le 30/07, personne ne l'importe) : `permigo-game/src/pages/eleve/trophees.js`. Aucune écriture DB, aucune migration. `npm run build` vert ; suite e2e ciblée (smoke/empty-states/accueil-gestalt, port dédié 5180) : 14/18 passés, les 4 échecs sont pré-existants et sans rapport (bouton fermeture fiche `parcours.js` non touché, sélecteur `#arn-notif` déjà absent du profil avant cette passe — toggle push déplacé vers `#/settings` lors de la refonte du 06/08 ci-dessus, test jamais mis à jour). | Impact : nav élève (4 fichiers additifs) + router partagé (additif, `ROUTES_RETIREES`) | status: DONE
- `[2026-08-06 permigo-eleve-dev]` **Refonte profil élève, variante B « Le paquet » (maquette `permigo-game/mockups/profil-refonte/profil-B-paquet.html`, décision Rayan)** : `permigo-game/src/pages/common/profil.js` **FICHIER PARTAGÉ élève/moniteur** — modif strictement confinée à `mountEleveArene()`/`_wireEleveArene()` (le chemin élève), `mountEnseignantArene()` **non touché** (vérifié par relecture + build). L'identité (avatar+pseudo+nom) tient dans une barre de 44px tout en haut ; « Mes cartes » (le paquet REMC) devient le héros de la page ; les trophées tombent de 13 à 2 (`comp_28`/`comp_31`, seuls jalons qui marquent un vrai moment de la conduite — filtrés à l'AFFICHAGE via une nouvelle constante locale `PROFIL_TROPHIES`, le `CATALOG` partagé de `data/achievements.js` n'est PAS modifié, donc rien ne casse ailleurs) ; le titre de monde, le badge « Permis B », le bloc « X/31 validations », la barre de compétence en cours et les tuiles Restantes/Série disparaissent (redondants avec le paquet et avec l'accueil) ; « Ma vitrine » (skins avatars) apparaît en aperçu grille 6 tuiles (états équipé/possédé/achetable/verrouillé, lecture seule via le RPC `get_items_catalog` déjà utilisé par `boutique.js`, un tap renvoie vers `#/boutique` où vit le vrai flux d'achat/équipement — pas dupliqué). **Le paquet n'est PAS dupliqué** : `permigo-game/src/pages/eleve/collection.js` (scope élève exclusif) exporte désormais `CARD_DECK_STYLE` (le bloc CSS du deck, extrait de son `STYLE` existant) + `cardEl`/`buildDeck`/`wireControls`/`enableTilt`/`loadSeen` (mécanique swipe/tap/flip inchangée, comportement identique à `#/cartes`) ; `mountEleveArene` importe ce module **dynamiquement** (`import("@/pages/eleve/collection.js")`, même pattern que `mon-permis.js`→`parcours.js`/`examen.js`/`accueil.js`) pour ne pas alourdir le chunk `profil` chargé par le moniteur, qui ne voit jamais ce code. Le geste change légèrement du fait de cette réutilisation stricte : la carte du paquet se RETOURNE en place pour montrer la méthode (comme sur `#/cartes`) plutôt que de s'ouvrir en plein écran comme la maquette le montrait — trade-off documenté, zéro nouvelle mécanique de swipe à maintenir. Cadre avatar : le crest 88px à 2 anneaux concentriques (corrigé le 06/08 dans l'entrée juste au-dessus) est **retiré avec le reste de la « carte de joueur »** — c'est la refonte demandée par Rayan, pas une régression : le nouvel avatar 44px utilise un simple `box-shadow` en anneau (aucune technique de masque, donc aucun retour possible du bug de liseré). Fonctionnalités préservées mais déplacées : le toggle de notifications push (redondant, la même bascule vit déjà sur `#/settings`) est retiré du profil ; le lien « Mes notifications » (`#/notifications`, seule porte d'accès élève à cette route avant cette passe) est conservé en ligne discrète sous la vitrine. Lecture seule DB (`profiles`, `validations`, `self_validations` — ajout de `validated_at` à la sélection existante —, RPC `get_my_achievements`/`get_items_catalog`), aucune écriture, aucune nouvelle route, aucune migration. ⚠️ Gap connu : les nouveaux libellés (« Mes cartes », « Prochaine carte », « Ma vitrine », tags de la vitrine…) sont en FR seulement, pas encore dans les dictionnaires EN/AR de `PROF_I18N` — repli silencieux sur le FR (même comportement dégradé que documenté dans la mémoire i18n du projet), aucun crash. | Impact : profil élève uniquement (moniteur/gérant lisent un chemin de fonction différent, non modifié) | status: DONE

---

## Historique des breaking changes

> Format : `[YYYY-MM-DD] avant → après | raison`

(vide)

### Changements en cours — 2026-06-10 (bot élève)
- **Ajout additif** `permigo-game/src/services/daily-quiz.js` (sélection « question du jour ») + flag `daily` sur la route `#/quiz/{comp}/{type}/daily` + carte action du jour dans `accueil.js`. Lecture seule de `validations`/`quiz_attempts` (RLS own). Aucun impact moniteur/gérant.

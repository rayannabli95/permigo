# Plan d'intégration des sons — 2026-06-04

6 fichiers dans `nouveau son/`. Le moteur existe déjà : `permigo-game/src/utils/sound.js`
joue les fichiers depuis `permigo-game/public/sounds/{nom}.mp3`.

Règle simple :
- **Copier** ton mp3 dans `permigo-game/public/sounds/` avec le nom cible ci-dessous.
- Si "remplacer" → tu écrases le fichier existant, **rien à coder**, c'est déjà branché partout.
- Si "nouveau" → en plus du fichier, il faut 2-3 petites lignes de code (je m'en occupe).

---

## 1. `musique connexion permigo.mp3`
- **Cible** : `public/sounds/transition.mp3` (remplacer)
- **Fonction** : `playLaunch()`
- **Déjà déclenché dans** : `src/pages/auth/login.js`, `src/pages/public/signup.js`
- **Action** : copier → écraser. Rien à coder.
- Note : joué 1× par session, après le 1er clic (sécurité autoplay navigateur).

## 2. `reponse corecte.mp3`
- **Cible** : `public/sounds/success.mp3` (remplacer)
- **Fonction** : `playCorrect()`
- **Déjà déclenché dans** : `quiz-engine.js`, `flash-quiz.js`, `exam-blanc.js`
- **Action** : copier → écraser. Rien à coder.

## 3. `musique notif .mp3`
- **Cible** : `public/sounds/notify.mp3` (remplacer)
- **Fonction** : `playNotify()`
- **État** : fonction existe mais peu/pas appelée → à brancher sur les toasts/notifs si tu veux l'entendre.
- **Action** : copier → écraser. Câblage optionnel (à voir ensemble selon où tu veux le son).

## 4. `musique dans le parcours eleve et enseignant .mp3`
- **Cible** : `public/sounds/parcours.mp3` (remplacer)
- **Fonction** : `playParcours()` (joué 1× par session)
- **Déjà déclenché côté élève** : `src/pages/eleve/parcours.js` ✓
- **Manque côté enseignant** : `src/pages/enseignant/parcours-pro-complet.js` → ajouter l'appel
- **Action** : copier → écraser **+ 2 lignes** à ajouter côté enseignant (import + appel dans `mount`).

## 5. `musique de fin lors de reussite quiz recap et exam blanc et nouvelle competence deblouqer.mp3`
- **Cible** : `public/sounds/victory.mp3` (**NOUVEAU**)
- **Fonction** : `playVictory()` (**à créer** dans `sound.js`)
- **À déclencher dans 3 endroits** :
  1. `exam-blanc.js` L291 — branche `if (passed)` (remplace l'actuel `playFanfare()`)
  2. `quiz-engine.js` ~L123 — fin de quiz recap réussi
  3. `quiz.js` — quand une **compétence est validée/débloquée** (remplace/double `playLevelup()`)
- **Action** : copier le nouveau fichier **+ nouvel export + 3 points de câblage** (je code).

## 6. `musique defaite exam ou quizz recap.mp3`
- **Cible** : `public/sounds/defeat.mp3` (**NOUVEAU**)
- **Fonction** : `playDefeat()` (**à créer** dans `sound.js`)
- **À déclencher dans 2 endroits** :
  1. `exam-blanc.js` L291 — branche `else` (aujourd'hui c'est `playWrong()`, qui est juste le buzz d'une question, pas une vraie défaite de fin)
  2. `quiz-engine.js` — fin de quiz recap **échoué**
- **Action** : copier le nouveau fichier **+ nouvel export + 2 points de câblage** (je code).

---

## Récap rapide

| # | Ton fichier | Nom cible dans public/sounds/ | Type | Code à écrire |
|---|---|---|---|---|
| 1 | musique connexion permigo | `transition.mp3` | remplacer | non |
| 2 | reponse corecte | `success.mp3` | remplacer | non |
| 3 | musique notif | `notify.mp3` | remplacer | optionnel |
| 4 | musique parcours élève+enseignant | `parcours.mp3` | remplacer | +2 lignes (côté moniteur) |
| 5 | musique de fin réussite | `victory.mp3` | **nouveau** | export + 3 branchements |
| 6 | musique défaite | `defeat.mp3` | **nouveau** | export + 2 branchements |

Les noms ont accents/espaces → ils seront renommés proprement à la copie (le moteur exige des noms simples sans espace).
